package com.compensatuviaje.tracker.feature.connectivity

import com.google.common.truth.Truth.assertThat
import org.junit.Test

class ConnectivityModuleTest {
    @Test fun `placeholder passes`() {
        assertThat(ConnectivityModule()).isNotNull()
    }
}
